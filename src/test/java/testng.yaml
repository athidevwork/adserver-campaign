name: Suite
configFailurePolicy: skip
tests:
  - name: Test
    classes:
      - testng.AdServerCampaignTest

  - name: ListenerClient
    classes:
      - com.aiblockchain.client.AIBlockChainListenerClientTest

